runtime: nodejs10
instance_class: B1
manual_scaling:
  instances: 1

handlers:
  - url: /public
    static_dir: public
  
  - url: /static
    static_dir: static

  - url: /client-assets
    static_dir: client-assets